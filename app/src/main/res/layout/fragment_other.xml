<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".fragments.OtherFragment">

    <ScrollView
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical"
        android:background="@color/bg_dark"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintBottom_toBottomOf="parent">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="10dp"
            android:paddingBottom="?actionBarSize"
            android:orientation="vertical"
            android:layout_gravity="top"
            android:gravity="center">

            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="horizontal"
                android:layout_gravity="center"
                android:gravity="center">

                <Button
                    android:id="@+id/telawat"
                    android:elevation="@dimen/buttony_elevation"
                    android:background="@drawable/ripple_s"
                    android:text="@string/recitations"
                    android:drawableTop="@drawable/ic_headset"
                    style="@style/Buttony"/>

                <Button
                    android:id="@+id/quiz"
                    android:elevation="@dimen/buttony_elevation"
                    android:background="@drawable/ripple_s"
                    android:text="@string/quiz_title"
                    android:drawableTop="@drawable/ic_quiz"
                    style="@style/Buttony"/>

            </LinearLayout>

            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="horizontal"
                android:layout_gravity="center"
                android:gravity="center">

                <Button
                    android:id="@+id/quran_searcher"
                    android:elevation="@dimen/buttony_elevation"
                    android:background="@drawable/ripple_s"
                    android:text="@string/search_in_quran"
                    android:drawableTop="@drawable/ic_quran_search"
                    style="@style/Buttony"/>

                <Button
                    android:id="@+id/channels"
                    android:elevation="@dimen/buttony_elevation"
                    android:background="@drawable/ripple_s"
                    android:text="@string/quran_and_sunnah_channels"
                    android:drawableTop="@drawable/ic_television"
                    style="@style/Buttony"/>

            </LinearLayout>

            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="horizontal"
                android:layout_gravity="center"
                android:gravity="center">

                <Button
                    android:id="@+id/settings"
                    android:elevation="@dimen/buttony_elevation"
                    android:background="@drawable/ripple_s"
                    android:text="@string/settings"
                    android:drawableTop="@drawable/ic_settings"
                    style="@style/Buttony"/>

                <Button
                    android:id="@+id/features"
                    android:elevation="@dimen/buttony_elevation"
                    android:background="@drawable/ripple_s"
                    android:text="@string/features_guide"
                    android:drawableTop="@drawable/ic_features"
                    style="@style/Buttony"/>

            </LinearLayout>

            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="horizontal"
                android:layout_gravity="center"
                android:gravity="center">

                <Button
                    android:id="@+id/contact"
                    android:elevation="@dimen/buttony_elevation"
                    android:background="@drawable/ripple_s"
                    android:text="@string/contact"
                    android:drawableTop="@drawable/ic_mail"
                    style="@style/Buttony"/>

                <Button
                    android:id="@+id/share"
                    android:elevation="@dimen/buttony_elevation"
                    android:background="@drawable/ripple_s"
                    android:text="@string/share_app"
                    android:drawableTop="@drawable/ic_share"
                    style="@style/Buttony"/>

            </LinearLayout>

            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="horizontal"
                android:paddingBottom="15dp"
                android:layout_gravity="center"
                android:gravity="center">

                <Button
                    android:id="@+id/about"
                    android:elevation="@dimen/buttony_elevation"
                    android:background="@drawable/ripple_s"
                    android:text="@string/about"
                    android:drawableTop="@drawable/ic_info"
                    style="@style/Buttony"/>

            </LinearLayout>

        </LinearLayout>
    </ScrollView>

</androidx.constraintlayout.widget.ConstraintLayout>