<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/compass_calibration_parent"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:layout_gravity="center"
    android:gravity="center"
    android:orientation="vertical">

    <ImageView
        android:id="@+id/gif_screen"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:contentDescription="@string/compass_calibration_gif_description"
        android:src="@drawable/compass_calibration" />

    <TextView
        style="@style/MyTV"
        android:layout_marginTop="-150dp"
        android:paddingHorizontal="35dp"
        android:text="@string/qibla_warning"
        android:textColor="@android:color/holo_red_dark"
        android:textSize="25sp"
        android:textStyle="bold" />

</LinearLayout>