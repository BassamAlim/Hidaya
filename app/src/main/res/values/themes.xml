<resources>
    <!-- Base application theme. -->
    <style name="Theme.Hidaya" parent="Theme.AppCompat.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/dark_primary</item>
        <item name="colorPrimaryVariant">@color/dark_primary</item>
        <item name="colorOnPrimary">@color/dark_primary</item>
        <!-- Secondary brand color. -->
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor">@color/dark_bg</item>
        <!-- Customize your theme here. -->
        <item name="android:textColorPrimary">@color/text</item>
        <item name="android:textColor">@color/text</item>
        <item name="colorAccent">@color/dark_accent</item>
        <item name="titleTextColor">@color/white</item>
    </style>

    <style name="QuranDarkTheme" parent="Theme.Hidaya">
        <!--<item name="background">@color/dark_bg</item>-->
        <item name="android:backgroundTint">@color/dark_primary</item>
        <!--<item name="backgroundColor">@color/dark_bg</item>
        <item name="colorOnPrimary">@color/dark_bg</item>
        <item name="colorAccent">@color/dark_bg</item>
        <item name="colorOnBackground">@color/dark_bg</item>
        <item name="colorOnSecondary">@color/dark_bg</item>
        <item name="colorOnContainer">@color/dark_bg</item>
        <item name="colorContainer">@color/dark_bg</item>
        <item name="colorOnPrimaryContainer">@color/dark_bg</item>-->
    </style>

    <style name="QuranLightTheme" parent="Theme.Hidaya">

    </style>

    <style name="LaunchTheme" parent="Theme.AppCompat.NoActionBar">
        <item name="android:windowBackground">@drawable/splash</item>
    </style>

    <style name="Theme.Hidaya.AppWidgetContainerParent" parent="@android:style/Theme.DeviceDefault">
        <item name="appWidgetRadius">8dp</item>
        <item name="appWidgetPadding">4dp</item>
        <item name="appWidgetInnerRadius">4dp</item>
    </style>

    <style name="Theme.Hidaya.AppWidgetContainer" parent="Theme.Hidaya.AppWidgetContainerParent" />

</resources>